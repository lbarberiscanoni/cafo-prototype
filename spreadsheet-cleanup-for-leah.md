# Spreadsheet Cleanup Checklist for Leah

Per-state list of column-header **renames** to make in `MTE_Metrics_2025.xlsx` so the dashboard parser picks up data it's currently dropping. The cross-cutting normalization you already did fixed most states; what remains are state-specific metric columns that still use non-canonical names.

## Canonical column names

Use these exact strings (case-sensitive) in the header row. Only rename columns that contain the corresponding data — don't add empty columns for metrics your state doesn't track.

| Metric | Canonical column name |
|---|---|
| Geography (county / region label) | `County`, `Region`, or `District Office` |
| Children in care | `Number of Children in Care` |
| Children in foster care | `Number of Children in Foster Care` |
| Children in kinship care | `Number of Children in Kinship Care` |
| Foster + kinship homes | `Number of Foster and Kinship Homes` |
| Children waiting for adoption | `Number of Children Waiting for Adoption` |
| Family preservation cases | `Number of Family Preservation Cases` |
| Reunification rate | `Biological Family Reunification Rate` |
| Adoptions count | `Number of Adoptions` |
| Time to adoption (months) | `Time Elapsed to Adoption (Months)` |
| Churches | `Number of Churches` |

## Per-state renames needed

### Delaware

| Current header | → | Canonical name |
|---|---|---|
| `Number of Family Preservation Cases (FY 2024)` | → | `Number of Family Preservation Cases` |

### Minnesota

| Current header | → | Canonical name |
|---|---|---|
| `Number of children in non-relative family foster placement` | → | `Number of Children in Foster Care` |
| `Number of children in relative family foster placement` | → | `Number of Children in Kinship Care` |
| `Active Child Foster Care Licenses 12/31/2024` | → | `Number of Foster and Kinship Homes` |

### Mississippi

| Current header | → | Canonical name |
|---|---|---|
| `Number of Family Preservation Cases (by Child)` | → | `Number of Family Preservation Cases` |

## Other unrecognized columns (please review)

These columns aren't mapped to any canonical metric. Most are likely intermediate calculations or state-specific alternates that don't need to change. Worth a quick look in case any of them represent a metric we're trying to surface — especially Hawaii's, which use different definitions (Permanent Custody award rather than Termination of Parental Rights, exit counts rather than rates) than the canonical metrics.

- **Hawaii**: `Number of Children Who Exited Foster Care`, `Number of Children Who Exited Foster Care to Adoption`, `Number of Children Who Exited Foster Care to Reunification`, `Average Number of Months from Award of PC to Adoption`
- **Louisiana**: `Bio Reunifications`, `Exits`
- **Maryland**: `Allegany`
- **Ohio**: `Bio Reunifications`, `Exits`

---

Generated by comparing `src/data/MTE_Metrics_2025.xlsx` headers against the parser's accepted column names in `src/data/parse-metrics.js`. **3 state(s) need renames**; 4 have additional unrecognized columns to review; the rest (44) are already clean.
